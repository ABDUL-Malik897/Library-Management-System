const UserModel = require('./user-model');
const BookModel = require('./book-model');

module.exports = {
    UserModel ,
    BookModel
}   
// refer to this file in index.js and import the models from here instead of importing them separately in index.js